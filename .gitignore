HELP.md
.gradle
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
!**/src/test/**/build/

### Kotlin ###
*.class
.kotlin/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache
bin/
!**/src/main/**/bin/
!**/src/test/**/bin/

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr
out/
!**/src/main/**/out/
!**/src/test/**/out/

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

### VS Code ###
.vscode/

### Gradle ###
gradle.properties

### Docker ###
docker-compose.override.yml

### Environment ###
.env
deploy/aws/.state
deploy/aws/*.pem
deploy/oci/.state
deploy/oci/*.pem
deploy/oci/*.pub
deploy/vultr/.state
deploy/vultr/*.pem
deploy/vultr/*.pub

### OS ###
.DS_Store
Thumbs.db

### Logs ###
*.log
hs_err_pid*
replay_pid*
# Ignore .claude/ contents EXCEPT versioned artifacts (task plans).
# Using .claude/* rather than .claude/ so git still descends into the
# directory and the negation below can re-include specific subpaths.
.claude/*
# Keep task plans version-controlled so multi-session / multi-machine
# workflows can resume from them.
!.claude/tasks/
# Same for §10 handoff plans — track them so worktree removal doesn't lose
# the decision record (see CLAUDE.md "plan 버전관리").
!.claude/plans/

### Worktrees ###
.worktrees/

### LLM Wiki ###
# 외부 원문 보관소. 페이지(wiki/pages/)는 추적하되 원문 덤프는 추적하지 않는다.
# 주의: gitignored 라 worktree 삭제 시 함께 사라진다 — 재취득 가능한 원문만 둘 것 (wiki/WIKI.md §5).
wiki/raw/
