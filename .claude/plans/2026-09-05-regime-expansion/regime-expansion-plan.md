---
title: regime-expansion — 비중첩 7국면(2020-01~2023-11) 추가와 사전고정 확증 판정
status: done
started: 2026-09-05
updated: 2026-09-05
---

# Goal

선행 3작업([[parameter-search-2026-09]]·[[exit-resolution-verdict-2026-09]]·라이브 의미론 팔)은 전부 **사후 분석**이었다 —
후보를 만든 데이터의 요약통계를 이미 본 뒤에 통계량을 바꿨다. 이 작업은 **아무도 값을 보지 않은 데이터** 위에서
규칙을 먼저 못 박고 판정한다. 이 스레드에서 유일하게 확증(confirmatory)이 가능한 지점이다.

비중첩 200일 창 7개(2020-01-23 ~ 2023-11-22)를 시점 중립 규칙으로 수집하고, **라이브 의미론 팔**(진입·청산 모두
라이브 규약, 240분봉)에서 판정한다. 일봉으로 판정하면 이 스레드가 방금 무효화한 계기를 다시 쓰는 것이다.

라이브 파라미터는 바꾸지 않는다.

# Progress

- 2026-09-05 — worktree 생성(base `main@e7f77e1`). Upbit 240분봉이 2019-07 까지 조회됨을 실측 → 7창 전부 일중봉 판정 가능.
- 2026-09-05 — `REGIMES` 에 7창 추가. 로스터가 시대에 맞게 나온다(XEC·WAVES·ZIL·PUNDIX·BORA·MBL·BSV — 오늘의 승자가 아니다).
- 2026-09-05 — **근본 수정**: `--write` 가 기존 4국면까지 재수집하는 것을 발견(`fixture-regimes` plan Decisions 4 가 금지한 동작 —
  유니버스 선정이 *현재 상장* 마켓을 열거하므로 그 사이 상장폐지가 있으면 옛 구간 로스터도 바뀐다). 두 수집기에 `--only` 추가.
- 2026-09-05 — 추가 버그: `--only` 로 일부만 돌면 `gaps.json` 이 통째로 덮어써져 **돌지 않은 구간의 결측 기록이 사라진다**.
  병합하도록 수정하고 이번 실행분은 git 에서 복원해 병합했다(bull 8건 보존 확인).
- 2026-09-05 — 7창 D1 수집 완료. **기존 4국면·yearly fixture 는 바이트 단위로 불변**(git status 비어 있음) — 재수집이 같은 결과를 냈다.
- 2026-09-05 — 240분봉 7창 67,193봉 수집. `BacktestFixtures` 에 enum·로스터 핀 배선.
  **파급 차단**: `Regime.entries` 를 순회하던 `ResetPolicySweepTest`·`CandidateComparisonTest`(둘 다 wiki 인용 수치)를
  `PUBLISHED_FOUR` 고정 목록으로 바꿔, 국면 추가로 모집단이 11개로 조용히 바뀌는 것을 막았다.
- 2026-09-05 — **사전고정 판정 실행. 변형 A 가 통과했다** — 격차 +110.37%p, 5% 하한 +50.30, `P(격차≤0)=0.0011`,
  거래수 라이브와 동일(758 vs 758), 7/7 창 양수. 후보 E 도 통과했으나(+467.58, P=0.0000) **생존편향과 분리되지 않아 후보에서 내렸다**
  (아래 Decisions 4). wiki `query/trailing-arm-finding-2026-09` 로 남겼다.

# Next

없음 — PR #170 로 닫혔다. 라이브 무변경. **후속은 소액 전향 검증**(승격은 사람 승인 — [[strategy-evolution-expectations]]).

# Decisions

## 1) 판정 계기는 라이브 의미론 팔이다

일봉은 경로 의존 게이트(트레일링·부분청산)를 라이브와 다르게 잰다([[exit-resolution-verdict-2026-09]]).
신규 국면을 일봉으로 판정하면 새 데이터를 틀린 계기로 소비하는 것이라, 되돌릴 수 없다.

## 2) 창별 부호검정을 쓰지 않는다

"≥5/7 창 통과" 는 상관된 7개 관측 위의 부호검정이고, 이 스레드가 마켓축(8마켓, 실효 1.22)에서 이미 기각한 통계 형식이다.
7창은 하나의 크립토 매크로 사이클이라 서로 독립이 아니다. 대신 **7창을 이어붙인 pooled 날짜블록 부트스트랩** 하나로 판정한다.
창별 결과는 **진단 표기**로만 낸다.

## 3) 생존편향은 과거로 갈수록 심해지고, 측정 불가다

`krw_markets()` 가 **오늘 상장된** 마켓만 열거하므로 "그 시점 상위 8" 은 실제로 "2026년까지 살아남은 것 중 상위 8" 이다.
후보 풀 자체가 과거로 갈수록 얇아진다 — 미상장 제외 수가 p2020h2 220 / p2021h2 205 / p2023h1 189 이다(오늘 287개 기준).
상장폐지 종목은 Upbit API 가 404 라 표본에 넣을 수도, 편향 크기를 추정할 수도 없다. **결론에 반드시 병기한다.**

# Key Files

- `scripts/collect_backtest_fixtures.py` — `REGIMES` +7, `--only` 추가
- `scripts/collect_intraday_fixtures.py` — `WINDOWS` +7, `--only` 쉼표 구분
- `bot/src/test/kotlin/com/trading/bot/engine/BacktestFixtures.kt` — `Regime` enum·로스터 핀
- `bot/src/test/kotlin/com/trading/bot/engine/LiveSemanticsArm.kt` — 판정 계기(선행 작업 산출물)

# Blockers

없음.

# Acceptance

**아래는 결과를 보기 전에 커밋하는 사전고정이다. 실행 후 이 문구를 고치지 않는다.**

1. ✅ 7창 D1·240분봉 수집 완료(67,193 일중봉). 기존 4국면·yearly fixture **바이트 단위로 불변** 확인.
2. ✅ 판정 대상은 **둘뿐**이다 — 변형 A(라이브 + 트레일링 1.5/arm 0)와 후보 E(k0.3/TP off/SL7/트레일1.5/arm0).
   새 그리드 탐색을 하지 않는다.
3. ✅ **주 통계량**: 7창을 이어붙인 pooled 격차 `G = Σ(후보) − Σ(라이브)`(고정 노셔널 net pnl%, 라이브 의미론 팔).
4. ✅ **불확실성**: 청산 달력일 블록 부트스트랩, 블록 = 7창 전체의 청산일 합집합, B=20,000, seed 고정.
5. ✅ **발견 선언** — A `P=0.0011`·하한 +50.30 **통과**, E `P=0.0000`·하한 +276.82 통과(단 Decisions 4).
   원문:: `P(G ≤ 0) ≤ 0.0253`(후보 2개 Šidák) **그리고** 5% 하한 > 0. 둘 다 만족해야 한다.
6. ✅ 결과가 어느 쪽이든 그대로 보고한다. 창별 수치는 진단 표기이며 판정에 쓰지 않는다.
7. ✅ `./gradlew build` 통과(실행 971 / skip 18 / 실패 0) + wiki 검증 3종 통과.
8. ✅ 라이브 무변경 — `TradingProperties`·`deploy/` diff 0.

## 4) E 는 사전고정을 통과했지만 후보에서 내린다

사전고정에 **생존편향 통제가 없었다**. E 는 익절 상한을 아예 없앤 설정이라, "2026년까지 살아남은 것 중 상위 8" 표본에서
구조적으로 유리하다. 규칙을 결과 본 뒤 고치는 대신 **진단을 병기**했다 — 마켓 단순보유 수익률과 창별 격차 대조:

- **A 는 방향 무관**하게 7/7 양수이고, **최대 격차가 −63.1% 폭락장**(+33.41), 최소가 +155.7% 상승장(+1.91)이다
  — 생존편향 가설이 예측하는 것과 정반대다. 거래수도 라이브와 같다.
- **E 는 방향 의존적**이다. 최대 +230.55 가 +155.7% 창에, 유일한 음수 −15.57 이 +7.9% 창에 있다. 거래를 62% 늘린다.

따라서 **A 만 남긴다**. E 의 통과가 무엇의 산물인지 이 표본으로 가를 수 없다.

## 5) 산출물 이름을 브랜치명과 같게 짓지 않는다

리포트를 `regime-expansion.md` 로 뒀다가 wiki smoke 의 음성검사(브랜치명 문자열)에 걸렸다 —
검사를 약화시키지 않고 `seven-regime-adjudication.md` 로 이름을 고쳤다.

# Deferred

- **소액 전향 검증** — A 승격의 다음 단계. 통계력이 아니라 **편향 제거**가 목적이다(슬리피지·부분체결·실체결 tick).
  배선은 이미 돼 있다(`TRADING_TRAILING_STOP_PCT`·`TRADING_TRAILING_ARM_PCT` env). (높음)
- **A 의 G3 재평가** — A 는 선행 사전고정이 G3(plateau, 이웃 6/10)로 기각한 좌표다. 두 사전고정이 반대 방향을
  가리키므로, 이웃을 **지문 기준으로 접은 뒤** plateau 를 다시 세는 것이 맞는지 판정이 필요하다. (중간)
- **공유 잔고 시뮬레이션** — 마켓별 예산 독립 가정. 상관 0.796 인 날의 동시 진입 우위가 과대평가된다. (중간)
